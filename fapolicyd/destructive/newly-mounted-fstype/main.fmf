description: test the newly mounted fstype is actually watched
contact: Dalibor Pospíšil <dapospis@redhat.com>
adjust:
  - enabled: true
    when: destructiveTests == 1
require+:
  - library(ControlFlow/Cleanup)
  - library(ControlFlow/SessionControl)
  - library(distribution/tcf)
  - library(distribution/Log)
  - name: /fapolicyd/destructive/library
    type: library
    path: ../tests
duration: 5m
test: ./runtest.sh
